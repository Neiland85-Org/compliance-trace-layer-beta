{
  "name": "@trace/intelligence",
  "version": "1.0.0",
  "private": true,
  "description": "Trace Platform — Architecture intelligence engine (PROPRIETARY IP)",
  "type": "module",
  "scripts": {
    "validate": "tsc --noEmit",
    "test": "node --import tsx test/intelligence.test.ts"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.19.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
